<h1><!--トップページ--></h1>
<div class ="zentaitext-back">
  <div class="form">
  <%= form_with url: "/posts", method: :post, local: true do |form| %>
  <%= form.text_field :content %>
  <%= form.submit 'POST' %>
  <% end %>
   </div>
  <div class="background">
     
$ rubi <div class="post">
     </div>
     <% @posts.each do |post| %>
     <div class="post-content">
     <%= post.content %>
     </div>
     <% end %>
  </div>
 </div>